This is a gui interface using tkinter, numpy, and matplot library to generate vector fields in 3D. It also includes a second program which generates 2D 
vector fields and some other graphs made to model specific circumstances of harmonic dampening. 
